{
  "name": "mortgage-suite-los",
  "version": "53.1.0",
  "private": true,
  "description": "Self-contained mortgage income and renovation scenario suite",
  "scripts": {
    "build": "python tools/build-release-53.py",
    "test": "node test/run-all.js",
    "verify": "npm test && npm run build",
    "verify:browser": "node build/verify-v35.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
